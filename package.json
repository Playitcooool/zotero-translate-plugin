{
  "name": "zotero-translate-plugin",
  "version": "0.1.0",
  "description": "Zotero 划词翻译插件，调用本地 LLM API",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
